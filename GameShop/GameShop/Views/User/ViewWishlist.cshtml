@model GameShop.Models.Users.ViewWishlist
@{
    ViewData["Title"] = "View Wishlist";
}

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="~/css/site.css" rel="stylesheet" />
</head>
<body >
    @*<div class="row">
        @foreach (var wish in Model.Wishlist)
        {
            <div class="col-xl-4 col-lg-4 col-md-6" style="border-style: groove;background-color:white;shape-outside">
                <div class="product-caption">
                    @foreach (var game in Model.Games)
                    {
                        @if (game.Id == wish.GameId)
                        {
                            <div>
                                <img src="data:image/png;base64, @Model.Games.ImageFile" width="350" height="500" alt="" class="center" />
                            </div>
                            <div class="product-caption">
                                <h4 align="center" style="color:black">@Model.Games.Name </h4>
                                <h4 align="center" style="color:black">@Model.Games.Price $ </h4>
                            </div>
                        }
                    }
                </div>

            </div>
        }
    </div>*@
</body>